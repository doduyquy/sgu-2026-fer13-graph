inherits:
  - d7a_graph_swin_standalone.yaml

experiment:
  name: d7a_graph_swin_standalone_seed43

paths:
  resolved_output_root: output/d7a_graph_swin_standalone_seed43

environments:
  local:
    paths:
      resolved_output_root: output/d7a_graph_swin_standalone_seed43
  kaggle:
    paths:
      resolved_output_root: /kaggle/working/outputs/d7a_graph_swin_standalone_seed43

training:
  seed: 43
