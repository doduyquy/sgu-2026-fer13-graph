inherits:
  - d8b_face_aware_graph_swin_border020.yaml

experiment:
  name: d8b_face_aware_graph_swin_border020_bestacc_earlyloss

paths:
  resolved_output_root: outputs/d8b_face_aware_graph_swin_border020_bestacc_earlyloss

output:
  dir: outputs/d8b_face_aware_graph_swin_border020_bestacc_earlyloss

environments:
  local:
    paths:
      resolved_output_root: outputs/d8b_face_aware_graph_swin_border020_bestacc_earlyloss
  kaggle:
    paths:
      resolved_output_root: /kaggle/working/outputs/d8b_face_aware_graph_swin_border020_bestacc_earlyloss

checkpoint:
  save_best_metric: val_accuracy
  save_best_mode: max

early_stopping:
  monitor: val_loss
  mode: min

logging:
  run_name: d8b_border020_bestacc_earlyloss
