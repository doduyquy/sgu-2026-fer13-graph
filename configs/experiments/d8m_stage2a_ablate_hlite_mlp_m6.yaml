inherits:
  - d8m_stage2a_motif_graph_frozen_stage1h_lite.yaml

experiment:
  name: d8m_stage2a_ablate_hlite_mlp_m6

paths:
  graph_repo_path: artifacts/graph_repo
  resolved_output_root: outputs/stage2a_ablate_hlite_mlp_m6

stage2:
  num_selected_motifs: 6
  use_region_features: false

model:
  arch: pooled_mlp

data:
  batch_size: 8
  num_workers: 0
  persistent_workers: false
  prefetch_factor: null

training:
  epochs: 10

output:
  dir: outputs/stage2a_ablate_hlite_mlp_m6

environments:
  local:
    paths:
      graph_repo_path: artifacts/graph_repo
      resolved_output_root: outputs/stage2a_ablate_hlite_mlp_m6
