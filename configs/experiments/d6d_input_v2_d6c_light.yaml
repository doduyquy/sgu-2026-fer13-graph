inherits:
  - d6c_class_attended_objectives_light.yaml

experiment:
  name: d6d_input_v2_d6c_light

paths:
  graph_repo_path: artifacts/graph_repo_d6d_input_v2
  resolved_output_root: output/d6d_input_v2_d6c_light

environments:
  local:
    paths:
      graph_repo_path: artifacts/graph_repo_d6d_input_v2
      resolved_output_root: output/d6d_input_v2_d6c_light
  kaggle:
    paths:
      graph_repo_path: /kaggle/working/artifacts/graph_repo_d6d_input_v2
      resolved_output_root: /kaggle/working/outputs/d6d_input_v2_d6c_light

graph:
  node_feature_names:
    - intensity
    - x_norm
    - y_norm
    - gx
    - gy
    - grad_mag
    - local_contrast
    - grad_ori_cos
    - grad_ori_sin
    - local_mean_5x5
    - local_std_5x5
    - border_distance
  version: d6d_input_v2_node12

model:
  node_dim: 12
  edge_dim: 5
