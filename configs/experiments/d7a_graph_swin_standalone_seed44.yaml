inherits:
  - d7a_graph_swin_standalone.yaml

experiment:
  name: d7a_graph_swin_standalone_seed44

paths:
  resolved_output_root: output/d7a_graph_swin_standalone_seed44

environments:
  local:
    paths:
      resolved_output_root: output/d7a_graph_swin_standalone_seed44
  kaggle:
    paths:
      resolved_output_root: /kaggle/working/outputs/d7a_graph_swin_standalone_seed44

training:
  seed: 44
