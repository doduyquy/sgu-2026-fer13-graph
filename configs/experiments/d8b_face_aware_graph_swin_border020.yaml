inherits:
  - d8b_face_aware_graph_swin.yaml

experiment:
  name: d8b_face_aware_graph_swin_border020

paths:
  resolved_output_root: output/d8b_face_aware_graph_swin_border020

output:
  dir: output/d8b_face_aware_graph_swin_border020

environments:
  local:
    paths:
      resolved_output_root: output/d8b_face_aware_graph_swin_border020
  kaggle:
    paths:
      resolved_output_root: /kaggle/working/outputs/d8b_face_aware_graph_swin_border020

loss:
  pixel_gate_border_weight: 0.02
