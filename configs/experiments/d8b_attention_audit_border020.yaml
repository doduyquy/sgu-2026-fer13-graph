inherits:
  - d8b_face_aware_graph_swin_border020.yaml

experiment:
  name: d8b_attention_audit_border020

audit:
  checkpoint: output/d8b_face_aware_graph_swin_border020/checkpoints/best.pth
  graph_repo_path: artifacts/graph_repo
  split: val
  output_dir: outputs/d8b_attention_audit_border020
  max_batches: 30
  max_visualizations: 32
  border_width: 4
  outer_border_width: 6
  region_tau: 0.05
  map_names:
    - pixel_gate
    - window_gate
    - region_gate
    - class_region_attn_pred
    - class_region_attn_true
    - class_region_attn_mean
    - regular_window_attn
    - shifted_window_attn
