inherits:
  - d7a_graph_swin_region_transformer.yaml

experiment:
  name: d7a_graph_swin_region_transformer_layers2

paths:
  resolved_output_root: output/d7a_graph_swin_region_transformer_layers2

output:
  dir: output/d7a_graph_swin_region_transformer_layers2

environments:
  local:
    paths:
      resolved_output_root: output/d7a_graph_swin_region_transformer_layers2
  kaggle:
    paths:
      resolved_output_root: /kaggle/working/outputs/d7a_graph_swin_region_transformer_layers2

model:
  graph_swin:
    use_region_transformer: true
    region_layers: 2
    region_heads: 4
