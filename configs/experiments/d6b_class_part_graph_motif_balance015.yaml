inherits:
  - d6b_class_part_graph_motif_long100.yaml

experiment:
  name: d6b_class_part_graph_motif_balance015

paths:
  resolved_output_root: output/d6b_class_part_graph_motif_balance015

environments:
  local:
    paths:
      resolved_output_root: output/d6b_class_part_graph_motif_balance015
  kaggle:
    paths:
      resolved_output_root: /kaggle/working/outputs/d6b_class_part_graph_motif_balance015

loss:
  lambda_slot_div: 0.01
  lambda_border: 0.005
  lambda_slot_balance: 0.015
  lambda_slot_smooth: 0.0

training:
  epochs: 100
  early_stopping_patience: 20
  amp_init_scale: 1024
