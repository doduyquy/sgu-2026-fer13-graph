inherits:
  - d8b_face_aware_graph_swin.yaml

experiment:
  name: d8b_face_aware_graph_swin_window4

paths:
  resolved_output_root: output/d8b_face_aware_graph_swin_window4

output:
  dir: output/d8b_face_aware_graph_swin_window4

environments:
  local:
    paths:
      resolved_output_root: output/d8b_face_aware_graph_swin_window4
  kaggle:
    paths:
      resolved_output_root: /kaggle/working/outputs/d8b_face_aware_graph_swin_window4

model:
  graph_swin:
    window_size: 4
    shift_size: 2
    num_region_tokens: 36
