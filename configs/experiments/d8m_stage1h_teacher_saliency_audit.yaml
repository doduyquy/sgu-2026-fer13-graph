experiment:
  name: d8m_stage1h_teacher_saliency_audit

paths:
  graph_repo_path: artifacts/graph_repo
  resolved_output_root: outputs/d8m_stage1h_teacher_saliency_audit

data:
  graph_repo_path: null
  split: val
  batch_size: 4
  max_batches: 2
  max_samples: 8
  num_workers: 0
  pin_memory: false
  persistent_workers: false
  prefetch_factor: null
  chunk_cache_size: 8
  chunk_aware_shuffle: false

teacher:
  model_config: null
  checkpoint: null
  model_name: null
  saliency_method: input_gradient
  target: ground_truth
  fallback_to_attention: true

motif:
  config: configs/experiments/d8m_motif_discovery_stage1g_k16_region_clean.yaml
  checkpoint: outputs/d8m_motif_discovery_stage1g_k16_region_clean/checkpoints/best.pth

output:
  dir: outputs/d8m_stage1h_teacher_saliency_audit
