inherits:
  - d8m_stage2a_motif_graph_frozen_stage1g.yaml

experiment:
  name: d8m_stage2a_motif_graph_frozen_stage1g_diag10

paths:
  graph_repo_path: artifacts/graph_repo
  resolved_output_root: outputs/d8m_stage2a_motif_graph_frozen_stage1g_diag10

output:
  dir: outputs/d8m_stage2a_motif_graph_frozen_stage1g_diag10

environments:
  local:
    paths:
      graph_repo_path: artifacts/graph_repo
      resolved_output_root: outputs/d8m_stage2a_motif_graph_frozen_stage1g_diag10
