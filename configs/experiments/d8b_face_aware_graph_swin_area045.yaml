inherits:
  - d8b_face_aware_graph_swin.yaml

experiment:
  name: d8b_face_aware_graph_swin_area045

paths:
  resolved_output_root: output/d8b_face_aware_graph_swin_area045

output:
  dir: output/d8b_face_aware_graph_swin_area045

environments:
  local:
    paths:
      resolved_output_root: output/d8b_face_aware_graph_swin_area045
  kaggle:
    paths:
      resolved_output_root: /kaggle/working/outputs/d8b_face_aware_graph_swin_area045

loss:
  pixel_gate_area_target: 0.45
